SELECT CASE WHEN COUNT(*) > 1
                 THEN CHAR('CATMAINT UPDATE UNLDDN PI18738',80)
                 ELSE CHAR(' ',80)
       END
  FROM SYSIBM.SYSCHECKS
  WHERE DBID=6
    AND CHECKCONDITION LIKE '% IN (-1,-2) %';
