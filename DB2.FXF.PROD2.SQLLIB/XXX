SELECT USER AS USER,
       CURRENT TIMESTAMP AS BOXTSP,
       TIME(CURRENT TIMESTAMP) AS BOXTIME,
       TIME(CURRENT TIMESTAMP - CURRENT TIMEZONE) AS GMTTIME,
       CURRENT TIMEZONE  AS ZONE
FROM SYSIBM.SYSDUMMY1
WITH UR
